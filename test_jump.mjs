import { Engine } from './client/src/core/Engine.js';

// Remove the import assertion in ConfigLoader by mocking it out or patching it.
// Oh wait, I can just use a local copy of Engine.js and mock Config.
// Actually, it's easier to run the simulation using a modified simulate.mjs.
